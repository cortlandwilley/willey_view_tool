require "spec_helper"

describe WilleyViewTool do
  it "has a version number" do
    expect(WilleyViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
